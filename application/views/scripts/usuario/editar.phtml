<div id="content" class="clearfix">
	<form name="formulario" method="post" action="usuario/cadastro" id="formulario">
		<input type="hidden" name="usuario_id" id="usuario_id" value="<?php echo $this->usuario_id;?>">
		Nome: <input type="text" name="nome" id="Nnome" value="<?php echo $this->usuario_nome; ?>" /></br> </br>
		<?php 
			$checkedM = "";
			$checkedF = "";
			
			if($this->usuario_sexo == "M"){
				$checkedM = "checked";			
			}elseif($this->usuario_sexo == "F"){				
				$checkedF = "checked";
			}?>
		Sexo: 
		<label>
			<input type="radio" name="sexo" id="sexoM" value="M" <?php echo $checkedM;?>/>
			Masculino
		</label>
		<label> 
			<input type="radio" name="sexo" id="sexoF" value="F" <?php echo $checkedF;?>/>
			Feminino
		</label></br> <br /> 
		Login: <input type="text" name="login_C" id="login_C" value="<?php echo $this->usuario_login; ?>" /> * informe um login possuindo de 5 a 15 caracteres<br /> <br />
	    Senha: <input type="password" name="senha_C" id="senha_C" value="" placeholder="**********" /> * a senha deve conter, no minimo, 8 caracteres <br /> <br />
	    Confirme a senha: <input type="password" name="confirma_C" id="confirma_C" value="" placeholder="**********" /><br /><br /> 
	    <input type="submit" value="Atualizar" onclick="verificaErro_cadastro();" />
	</form>
	<script type="text/javascript">
		
	// fun��o que verifica os erros de valida��o do formul�rio
	function verificaErro_cadastro(){
	
        var erro_cadastro = false;
	// verificar limites de caracteres do login
	if(document.getElementById('login_C').value.length < 5 || 
           document.getElementById('login_C').value.length > 15){
           alert('O login do cadastro foi informado incorretamente!');
           document.getElementById('login_C').focus();
           erro_cadastro = true;
			}
    function verifica_cadastro(){
        if(document.getElementById('sexoM').checked || 
            document.getElementById('sexoF').checked){
            alert('Seus dados do cadastro ser�o enviados.');
            document.formulario.submit();
	}
        
        else{
        }
			
alert('Voc� precisa marcar uma das op��es para se \n\
cadastrar (Masculino ou Feminino).');
			
                    }
	
	if(!erro_cadastro){
	// verificar limite de caracteres da senha
	if(document.getElementById('senha_C').value.length < 8){
	alert('A senha do cadastro foi informado incorretamente!');
	document.getElementById('senha_C').focus();
	erro_cadastro = true;
				 }
			}
			
        if(!erro_cadastro){
        // verificar se a senha e a confirma��o da senha sao iguais
        if(document.getElementById('senha_C').value 
        != document.getElementById('confirma_C').value){
	alert('A confirma��o da senha do cadastro difere da senha informada!');
	document.getElementById('senha_C').focus();
	erro_cadastro = true;
				 }
			}
// retorna verdadeiro caso tenha ocorrido algum erro de valida��o dos compos
return erro_cadastro;
		}
		
		
	</script>
</div>
